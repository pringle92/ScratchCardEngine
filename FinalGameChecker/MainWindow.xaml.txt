<Window x:Class="FinalGameChecker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinalGameChecker"
        xmlns:viewModels="clr-namespace:FinalGameChecker.ViewModels"
        xmlns:editor="clr-namespace:ScratchCardGenerator.Common.Views;assembly=ScratchCardGenerator.Common"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">

    <!-- Define the ViewModel for this View -->
    <Window.DataContext>
        <viewModels:CheckerViewModel/>
    </Window.DataContext>

    <!-- Define Resources, like Converters -->
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- =================================================================================== -->
        <!-- STEP 1: Create or Load the Checker Project File                                     -->
        <!-- =================================================================================== -->
        <GroupBox Grid.Row="0" Header="Step 1: Create or Load Checker Project File" FontWeight="Bold" Margin="5" Padding="10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Save Checker Project..." Command="{Binding SaveCheckerProjectCommand}" Margin="5" Padding="10,5" ToolTip="Save the manually created project file for re-use."/>
                    <Button Content="Load Checker Project..." Command="{Binding LoadCheckerProjectCommand}" Margin="5" Padding="10,5" ToolTip="Load a previously saved checker project file."/>
                </StackPanel>

                <TextBlock DockPanel.Dock="Top" TextWrapping="Wrap" Margin="0,0,0,10" FontStyle="Italic">
                    Use the controls below to manually recreate the project from the original specification. This file will be used as the "source of truth" for all checks.
                </TextBlock>

                <!-- Implemented: The shared ProjectEditorControl is now hosted here, bound to its own ViewModel. -->
                <editor:ProjectEditorControl DataContext="{Binding EditorViewModel}" />

            </DockPanel>
        </GroupBox>

        <!-- =================================================================================== -->
        <!-- STEP 2: Load the Original Project File from the Generator                         -->
        <!-- =================================================================================== -->
        <GroupBox Grid.Row="1" Header="Step 2: Load Original Generator Project File" FontWeight="Bold" Margin="5" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding OriginalProjectFilePath, Mode=OneWay}" IsReadOnly="True" VerticalContentAlignment="Center" Margin="5"/>
                <Button Grid.Column="1" Content="Load Original Project..." Command="{Binding LoadOriginalProjectCommand}" Margin="5" Padding="10,5"/>
            </Grid>
        </GroupBox>

        <!-- =================================================================================== -->
        <!-- STEP 3: Run Checks and View Results                                                 -->
        <!-- =================================================================================== -->
        <GroupBox Grid.Row="2" Header="Step 3: Run Checks" FontWeight="Bold" Margin="5" Padding="10">
            <Button Content="Run Comparison and Final Checks" 
                    Command="{Binding RunChecksCommand}" 
                    HorizontalAlignment="Stretch" 
                    Padding="10" 
                    FontWeight="Bold" 
                    FontSize="14"/>
        </GroupBox>

        <!-- =================================================================================== -->
        <!-- RESULTS AREA                                                                        -->
        <!-- =================================================================================== -->
        <GroupBox Grid.Row="3" Header="Results" FontWeight="Bold" Margin="5" Padding="10" MinHeight="150">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Overall Status Display -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Overall Status: " FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding OverallStatus}" FontWeight="Bold" FontSize="16" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="Black"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding OverallStatus}" Value="PASS">
                                        <Setter Property="Foreground" Value="Green"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding OverallStatus}" Value="FAIL">
                                        <Setter Property="Foreground" Value="Red"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

                <!-- Detailed Results / Differences List -->
                <ListView Grid.Row="1" Margin="5" ItemsSource="{Binding ComparisonDifferences}"
                          Visibility="{Binding HasDifferences, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Area" DisplayMemberBinding="{Binding Area}" Width="150"/>
                            <GridViewColumn Header="Property" DisplayMemberBinding="{Binding Property}" Width="150"/>
                            <GridViewColumn Header="Original Value" DisplayMemberBinding="{Binding OriginalValue}" Width="200"/>
                            <GridViewColumn Header="Checker Value" DisplayMemberBinding="{Binding CheckerValue}" Width="200"/>
                            <GridViewColumn Header="Technical Path" DisplayMemberBinding="{Binding TechnicalPath}" Width="300"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <!-- Status Bar and Diff Button -->
                <DockPanel Grid.Row="2" Margin="5">
                    <Button DockPanel.Dock="Right" Content="Show Visual Diff" 
                            Command="{Binding ShowDiffCommand}"
                            Padding="10,5"
                            Visibility="{Binding HasDifferences, Converter={StaticResource BooleanToVisibilityConverter}}"
                            ToolTip="Launches an external tool to show a side-by-side comparison of the project files."/>

                    <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" FontStyle="Italic" Foreground="Gray"/>
                </DockPanel>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
